# 🧪 الاختبارات المقترحة

## 📋 قائمة الاختبارات

### ✅ اختبار 1: تسجيل جديد كـ Buyer

**الخطوات:**
1. افتح http://localhost:5174 في المتصفح
2. اضغط "Sign Up" أو الرابط المشابه
3. ملأ النموذج:
   - Full Name: `أحمد العلي`
   - Email: `buyer@example.com`
   - Password: `password123`
   - Confirm Password: `password123`
   - Role: اختر "Buyer"
4. اضغط "Sign Up"

**النتيجة المتوقعة:**
- ✅ يتم الدخول مباشرة إلى Buyer Dashboard (`/buyer`)
- ✅ تشاهد واجهة المشتري
- ✅ في Console ستشاهد: `Login successful, user type: buyer`

---

### ✅ اختبار 2: تسجيل جديد كـ Seller

**الخطوات:**
1. افتح http://localhost:5174 في المتصفح
2. اضغط "Sign Up"
3. ملأ النموذج:
   - Full Name: `محمد البائع`
   - Email: `seller@example.com`
   - Password: `password123`
   - Confirm Password: `password123`
   - Role: اختر "Seller"
4. اضغط "Sign Up"

**النتيجة المتوقعة:**
- ✅ يتم الدخول مباشرة إلى Seller Dashboard (`/seller`)
- ✅ تشاهد Sidebar على اليسار والـ Dashboard
- ✅ في Console ستشاهد: `Login successful, user type: seller`

---

### ✅ اختبار 3: تسجيل دخول

**الخطوات:**
1. افتح http://localhost:5174/login
2. أدخل بيانات حساب:
   - Username: `buyer` أو `احمد علي` (من اختبار 1)
   - Password: `password123`
3. اضغط "Login"

**النتيجة المتوقعة:**
- ✅ يتم الدخول إلى Buyer Dashboard
- ✅ في Console: رسالة نجاح التسجيل

---

### ✅ اختبار 4: محاولة الدخول المباشر لمسار محمي (خاطئ)

**الخطوات:**
1. سجل دخول كـ Buyer
2. اكتب في الـ URL مباشرة: `http://localhost:5174/seller`
3. اضغط Enter

**النتيجة المتوقعة:**
- ✅ يتم إعادة التوجيه تلقائياً إلى `/buyer`
- ✅ لا يمكنك الدخول إلى Seller Dashboard

---

### ✅ اختبار 5: محاولة الدخول المباشر (صحيح)

**الخطوات:**
1. سجل دخول كـ Seller
2. اكتب في الـ URL: `http://localhost:5174/seller/listings`
3. اضغط Enter

**النتيجة المتوقعة:**
- ✅ تدخل إلى صفحة Seller Listings
- ✅ تشاهد Sidebar على اليسار

---

### ✅ اختبار 6: التحقق من localStorage

**الخطوات:**
1. سجل دخول إلى الموقع
2. اضغط F12 لفتح Developer Tools
3. اذهب إلى: Tabs → Application → LocalStorage → http://localhost:5174

**النتيجة المتوقعة:**
- ✅ يجب أن ترى `currentUser` مع بيانات المستخدم
- ✅ يجب أن ترى `wtv_access_token`
- ✅ يجب أن ترى `wtv_refresh_token`

**للتحقق من البيانات:**
```javascript
// في Console اكتب:
console.log(JSON.parse(localStorage.getItem('currentUser')))
// يجب أن تشاهد: {id: ..., username: ..., user_type: "buyer", ...}
```

---

### ✅ اختبار 7: التحقق من Console Logs

**الخطوات:**
1. افتح F12
2. اذهب إلى Console
3. سجل دخول جديد

**النتيجة المتوقعة:**
- ✅ رسالة: `"Registration successful, tokens received"`
- ✅ أو: `"Login successful for user: {object}"`
- ✅ أو: `"Registration successful, user type: buyer"`

---

### ✅ اختبار 8: Logout واختبار إعادة الدخول

**الخطوات:**
1. سجل دخول
2. اضغط Logout (إذا كان الزر موجود)
3. حاول الدخول المباشر إلى `/buyer` بدون تسجيل دخول

**النتيجة المتوقعة:**
- ✅ يتم التوجيه إلى `/login`
- ✅ في Console: `"No user found, redirecting to login"`

---

### ✅ اختبار 9: التبديل بين Buyer و Seller

**الخطوات:**
1. سجل دخول كـ Buyer في نافذة عادية
2. افتح نافذة جديدة (Incognito/Private) في نفس المتصفح
3. سجل دخول كـ Seller في النافذة الجديدة
4. في النافذة الأولى (Buyer): اضغط Refresh
5. في النافذة الثانية (Seller): اضغط Refresh

**النتيجة المتوقعة:**
- ✅ كل نافذة تحتفظ ببيانات المستخدم الخاصة بها
- ✅ الـ localStorage منفصل لكل نافذة

---

## 🐛 اختبارات التشخيص

### إذا لم يتم التوجيه:

**تحقق من:**
```javascript
// 1. هل البيانات محفوظة؟
localStorage.getItem('currentUser')
// يجب أن تعود بيانات

// 2. ما هو user_type؟
JSON.parse(localStorage.getItem('currentUser')).user_type
// يجب أن يكون "buyer" أو "seller"

// 3. ما الخطأ في Console؟
// ابحث عن رسائل خطأ في الـ Console
```

### إذا تم إعادة التوجيه الخاطئ:

```javascript
// 1. هل user_type محفوظ بشكل صحيح؟
// 2. هل ProtectedRoute تتحقق بشكل صحيح؟
// 3. جرب تحديث الصفحة (Ctrl+R)
```

---

## 📊 ملخص الاختبارات

| الاختبار | الوصف | النتيجة |
|---------|-------|--------|
| 1 | تسجيل جديد Buyer | يدخل /buyer |
| 2 | تسجيل جديد Seller | يدخل /seller |
| 3 | تسجيل دخول | يدخل الصفحة الصحيحة |
| 4 | دخول مباشر خاطئ | إعادة توجيه صحيحة |
| 5 | دخول مباشر صحيح | يدخل الصفحة |
| 6 | التحقق من localStorage | البيانات محفوظة |
| 7 | التحقق من Console | رسائل صحيحة |
| 8 | Logout | توجيه إلى login |
| 9 | تبديل المستخدمين | كل واحد له بياناته |

---

## ✨ ملاحظات مهمة

- 🟢 كل الاختبارات يجب أن تنجح
- 🟡 إذا فشل أي اختبار، تحقق من الـ Console للأخطاء
- 🔴 إذا استمرت المشاكل، تواصل معي بالتفاصيل
- 📝 احتفظ بلقطات الشاشة للمشاكل

---

**تاريخ الاختبار:** [ادخل التاريخ هنا]  
**النتيجة:** ✅ نجح / ❌ فشل  
**الملاحظات:** [ادخل الملاحظات هنا]
